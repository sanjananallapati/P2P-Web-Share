{
  "name": "beam-signaling-server",
  "version": "1.0.0",
  "description": "Lightweight WebRTC signaling server for Beam — coordinates the handshake, never touches file data.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "beam": "file:..",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
